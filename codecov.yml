codecov:
  notify:
    require_ci_to_pass: yes

coverage:
  precision: 2
  round: down
  range: "70...100"

  status:
    project:
      default:
        enabled: yes
        target: 75%
        threshold: 10%

    patch:
      default:
        enabled: yes
        target: 50%

changes: no
